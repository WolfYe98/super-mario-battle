export type Record = {
    username: string,
    punctuation: number,
    ufos: number,
    disposedTime: number,
    recordDate: number
}
export type Result = {
    punctuation: number,
    ufos: number,
    disposedTime: number
}